// La destructuracion de los arreglos se realiza utilizando [] para listar las variables asociadas a los items del mismo
const [goku, vegeta, trunks]: string[] = ['Goku', 'Vegeta', 'Trunks']

console.log(`Personaje 1: ${goku}`)
console.log(`Personaje 2: ${vegeta}`)
console.log(`Personaje 3: ${trunks}`)

export{}