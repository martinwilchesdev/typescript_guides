// La desestructuracion de los arreglos se realiza utilizando [] para listar las variables asociadas a los items del mismo
const heroes: string[] = ['Goku', 'Vegeta', 'Trunks']
const [goku, vegeta, trunks] = heroes

console.log(`Personaje 1: ${goku}`)
console.log(`Personaje 2: ${vegeta}`)
console.log(`Personaje 3: ${trunks}`)

export { }